#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n,m,k;
    cin >> n >> m >> k;
    vector<vector<int>> deals;
    for (int i =0;i<n;i++){
        string s;
        cin >> s;
        for (int j =0;j<m;j++){
            int buy,sell,quant;
            cin >> buy >> sell >> quant;
            
        }
    }
}